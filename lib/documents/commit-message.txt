style: reduce hero banner title size

- Adjusted the text size from text-[19px]/text-[21px] to text-[15px]/text-[16px] on mobile.
- Added `tracking-tight` and `sm:` breakpoints to ensure the title correctly fits on a single line on smaller screens.
