feat(ads): exclude jobs quiz/chat pages from TopAds and promise pages from offerwall

- Add jobs quiz and chat routes to all three TopAds exclusion lists
  (pageSetting, interstitial, offerwall) to suppress ads on user journey pages
- Exclude all 6 jobs promise pages from the offerwall to prevent conflict
  with the rewarded ad unit rendered on those pages
- Fix jobs layout to use static import for next/headers and force-dynamic export