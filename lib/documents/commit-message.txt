feat: add round-robin ad unit rotation for TopAdsPlacement

- Add optional `unitIndex` prop to TopAdsPlacement that generates
  container IDs as `square0{n}` (backward-compatible with `id` prop)
- Implement ad rotation in invit-credit-card-rec-us-3 quiz page:
  square01 → square02 → square03 → square01 on each user selection
- Use React key-based unmount/remount for clean ad provider detection
- Remove legacy refreshGPTSlot in-place refresh in favor of rotation
- Enhanced logging for rotation events and unitIndex tracking
