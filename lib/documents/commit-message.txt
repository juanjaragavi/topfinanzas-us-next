fix: whitelist quiz-overlay from orphan-cleanup CSS and revert offerwall to question-less scaffold

- Exempt #quiz-overlay from aggressive orphan overlay cleanup rules in globals.css
- Add immediate backdrop-filter blur(8px) and dark semi-transparent background to #quiz-overlay
- Condition main content blur-stripping rules to only apply when quiz-overlay is absent
- Revert offerwall-for-finance-direct.tsx to self-closing div with no quiz question children