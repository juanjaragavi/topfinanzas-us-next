fix: resolve Amazon Careers CTA redirect loop and enforce external routing for all job provider links

- Update makeAmazonCtaBanner() href from self-referencing /jobs/jobs-reward-p2 to https://www.amazon.jobs/
- Add makeIndeedCtaBanner() factory and use on jobs-reward-p1 to eliminate self-referencing CTA
- Update JobMethodCard to detect external URLs and render <a target="_blank" rel="noopener noreferrer"> instead of Next.js <Link>
- Update JobsCtaBanner with same external URL handling for provider links
- All 6 external provider cards (Indeed, ZipRecruiter, LinkedIn, Snagajob, Glassdoor, USAJobs) now bypass internal routing
