feat(jobs): defer TopAds fill until quiz completion

- keep deferred ad slots mounted and reveal them after jobsQuizDone
- trigger topAds.spa() only after revealed slots are painted
- update jobs quiz flows and related jobs pages/components for the new ad timing
- include supporting formatting and translation string cleanup in touched jobs files

