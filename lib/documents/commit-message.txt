feat: integrate finance offerwalls and bidirectional linking across jobs pages

- Add FinanceOfferwall (quiz-based) to jobs-promise-p1 with quiz config
- Add FinanceOfferwallDirect to jobs-promise-p2 with TopAdsQuizIdStamper
- Add finance quiz configs (jobsPromiseP1, jobsPromiseP2) to finance-quiz-config.ts
- Update promise P3-P6 CTAs with links to corresponding reward pages
- Add backLink prop to JobsRewardTemplate for return navigation
- Wire backLink on all 6 reward pages pointing to their paired promise page
