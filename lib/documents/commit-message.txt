fix: replace DOM unmount/remount ad strategy with GPT pubads refresh on rec-us-3

- Keep a single stable ad div to prevent removeChild NotFoundError
- Use googletag.pubads().refresh([slot]) to load fresh creative per step
- Fire both GPT slot refresh and TopAds SPA trigger on step transitions

