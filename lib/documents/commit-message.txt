fix: resolve edge runtime and font loading build errors in opengraph-image files

- Removed `export const runtime = 'edge'` from all opengraph-image.tsx files to fix static generation conflicts with `generateStaticParams`.
- Removed direct fetch logic for unsupported WOFF2 fonts in Satori `ImageResponse` to prevent Node.js parsing and runtime signature validation errors during prerendering.
- Verified full success of static generation for 117 pages via `npm run build`.
