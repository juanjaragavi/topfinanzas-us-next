feat(jobs): add minimal jobs layout system with route group overrides

- Add JobsHeader component (logo + "Opportunities" brand text, no nav links, translateText hook slot for future Google Translate integration)
- Add JobsFooter component (bg-gray-100, favicon lockup, full legal/disclosure content, zero nav links)
- Add JobsFloatingCta component ("use client", fires at ≥50% scroll depth, fixed bottom-right, sessionStorage-scoped dismissal, ARIA-labeled)
- Migrate all 16 jobs pages into Next.js route groups: (promise), (reward), (quiz), (chat)
- Add per-route-group layout.tsx overrides: promise/reward get JobsHeader + JobsFloatingCta + JobsFooter; quiz/chat get JobsHeader + copyright line only
- Remove global Header and CompactFooter from jobs-promise-template, jobs-reward-template, jobs-quiz-entry, jobs-chat-entry
- media.topfinanzas.com already whitelisted in remotePatterns (no config change needed)
- Zero TypeScript errors, zero ESLint violations, no console.log statements
- Refactor .github/copilot-instructions.md for conciseness
